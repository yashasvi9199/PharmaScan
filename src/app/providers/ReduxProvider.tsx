// src/app/providers/ReduxProvider.tsx
// Redux Provider (placeholder)

import type { ReactNode } from "react";

interface Props {
  children: ReactNode;
}

export default function ReduxProvider({ children }: Props) {
  return <>{children}</>;
}
